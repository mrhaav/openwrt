if [ "$PRODUCT" = '12d1/155e/102' ] && [ "$INTERFACE" = '10/0/1' ] && [ "$ACTION" = 'remove' ]
then
    logger -t hotplug 'Unplugged Huawei E3372h-320 modem'
    wanIface=$(uci show network | grep "proto='atc'" | awk -F '.' '{print $2}')
    ifdown $wanIface
fi

